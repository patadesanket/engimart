# Auto-normalize line endings to avoid CRLF/LF issues
* text=auto

# Force LF endings for source files (good for cross-platform teams)
*.js   text eol=lf
*.jsx  text eol=lf
*.css  text eol=lf
*.html text eol=lf
*.json text eol=lf
*.md   text eol=lf

# Treat images and binary files correctly
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.svg  text

# Node modules should be excluded from normalization (though they're gitignored)
node_modules/** -text
